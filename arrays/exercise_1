#Write a program that checks to see if the number appears in the array

arr = [1, 3, 5, 7, 9, 11]
number = gets.chomp.to_i

(arr.include?(number)) ? (puts "Yes, it's in there!") : (puts "No trace of that!")
