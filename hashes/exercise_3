#Using some of Ruby's built-in Hash methods, write a program that loops through a hash and prints all of the keys.
#Then write a program that does the same thing except printing the values. Finally, write a program that prints both.

family= {:parents=>["mary", "jean-claude", "monique"], :uncles=>["bob", "joe", "steve"], :sisters=>["jane", "jill", "beth"], :brothers=>["frank", "rob", "david"], :aunts=>["mary", "sally", "susan"]}

#first : print only keys
family.each { |keys, values| puts keys }

#second : print only values

family.each { |keys, values| puts values }

#third : print both

family.each { |keys, values| p keys, values }
